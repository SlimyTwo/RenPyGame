# Initialize the factory package
# This is an empty __init__.py file, which allows Python to recognize this directory as a package.
# You can also import specific classes or functions here if needed.
from .ButtonFactory import MenuButtonFactory

__all__ = ['MenuButtonFactory']
